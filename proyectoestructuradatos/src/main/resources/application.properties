 Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/plataforma_educativa
spring.datasource.username=postgres
spring.datasource.password=123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Entity scan path
spring.jpa.properties.hibernate.packages-to-scan=com.plataformaEducativa.proyectoestructuradatos.entity

# JWT Configuration
jwt.secret=4dusoc14LS3cr3tK3yForJWT4uth3nt1c4tion
jwt.expiration=86400000

# Server Configuration
server.port=8080
server.servlet.context-path=/api/v1

# Logging Configuration
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Flyway Configuration (desactivado para depurar problemas iniciales)
spring.flyway.enabled=false
